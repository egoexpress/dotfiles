#!/usr/bin/env zsh

print -P "› %F{208}state of .dotfiles%f"
git --git-dir=$DOTFILES/.git --work-tree=$DOTFILES status -s

if [ -d ~/.private/.git ]; then
  print -P "› %F{208}state of .private%f"
  git --git-dir=$HOME/.private/.git --work-tree=$HOME/.private status -s
fi
